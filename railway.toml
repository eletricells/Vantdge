# Railway configuration for Vantdge
# This file configures the default service settings

[build]
builder = "nixpacks"

[deploy]
# Healthcheck disabled for Streamlit (doesn't have /health endpoint)
# healthcheckPath = "/health"
# healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

