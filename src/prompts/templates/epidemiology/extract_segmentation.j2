<task>
Extract patient segmentation data for "{{ disease }}" by {{ seg_type }} from these search results.
</task>

<search_results>
{{ formatted_results }}
</search_results>

<instructions>
For each segment, extract:
- Segment name (e.g., "Mild", "Moderate", "Severe")
- Description
- Patient count or percentage
- Year of data
</instructions>

<output_format>
Return JSON array:
[
    {
        "segment_name": "...",
        "segment_description": "...",
        "percentage_of_total": <number 0-100>,
        "data_year": <year>,
        "confidence_score": <0.0-1.0>
    }
]

If no segmentation data found, return empty array [].
Return ONLY valid JSON, no other text.
</output_format>

