List the standard clinical endpoints used in {{ indication }} clinical trials.

Include:
- Primary efficacy endpoints (e.g., ACR20/50/70 for RA, SLEDAI for SLE)
- Key secondary endpoints
- Safety endpoints commonly measured

Return as a JSON list: ["endpoint1", "endpoint2", ...]

Be specific to {{ indication }}.

