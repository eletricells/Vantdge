## JSON OUTPUT RULES

CRITICAL: Return ONLY valid JSON. No markdown, no code blocks, no explanations.

Rules:
1. Use double quotes for all strings
2. No trailing commas before closing brackets ] or braces }
3. Escape special characters properly (use \" for quotes inside strings)
4. All string values must be on a SINGLE LINE (no newlines within strings)
5. Use null for missing/unknown values (don't omit fields)
6. Ensure all arrays and objects are properly closed

DO NOT wrap the JSON in code blocks. Return raw JSON only.

